
<h1>The $Squad.getName() Heroes:</h1>

#if ($Squad.getHeroes().size()> 0)
<p>
  #foreach($hero in $Squad.getHeroes())
   <p><a href="/heroes/$hero.getId()">$hero.getName()</a></p>
  #end
<p>
#else
 <p> <em>No Heroes were found!</em></p>
#end
<a href="/squads/$squad.getId()/newhero"><button type="submit" class="btn btn-success">Add new hero</button></a><br><br>

<a href="/squads"><button type="submit" class="btn btn-success">view Squads</button></a>
